import Foundation

// We need to access ParityModels and JourneyTileGenerator.
// Let's create a swift script inside the package context or run it using swift build dependencies if needed.
// Wait, we can compile a file inside the package test or just use swift repl.
